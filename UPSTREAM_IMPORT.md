# 外部 Provider 审计策略

## 审计来源

Provider 审计有两类来源，门禁强度不同：

- **Provider preset 基线**（受 hash 门禁）：仓库位置由 `CC_SWITCH_PROVIDER_AUDIT_ROOT` 指定，默认同级 `../cc-switch`；只按固定 commit 只读 Git blob，快照见 `assets/contract/upstream-provider-source-baseline.json`。
- **协议行为证据**（人工审计，不入门禁）：外部参考实现与厂商协议文档，仅用于发现 Claude、Codex、Gemini 等 Provider 类型和协议行为变化。

本仓库不做整仓 merge，也不从外部仓库同步 UI、locale、运行时命令或页面结构。任何变更都必须先形成 Server 需求与契约，再在本仓库独立实现和验收。

当前 Provider 审计输入固定为 commit `b1dee0153da94316fb50416c679a11f74cc66f14`。五个必查文件的内容 hash、AST 抽取结果和 Universal recipe 保存在 `assets/contract/upstream-provider-source-baseline.json`；Server 当前 16/8/5 legacy Web preset 单独保存在 `assets/contract/server-provider-legacy-inventory.json`，两者都不是 Web runtime 真值。当前运行时真值是 43 个 Rust Provider Profiles，其中 `claude.google_oauth`、`codex.kiro_oauth` 与 Claude/Codex/Gemini 三端 Kimi Code Profiles 是经过 review 的 Server 首类增量；`claude.anthropic_bearer_relay` 是 `claude.custom_http` 下经过 review 的快捷配方，不再占用独立 Family/Profile。以上增量均不回写或伪装成 upstream preset。需要更新审计快照时运行 `node scripts/audit/audit-upstream-provider-baseline.mjs --refresh-source`；该命令只更新 Provider 证据，不复制或修改实现代码。

## 必须审计

Provider 基线仓库出现以下路径变更时必须审查。其中带 † 的五个文件同时受 hash 门禁（内容 hash 记录在 baseline 快照里，变更会被审计脚本检出）；其余为人工复查项，不进门禁：

- `src-tauri/src/proxy/forwarder.rs`
- `src-tauri/src/proxy/handlers.rs`
- `src-tauri/src/proxy/providers/**` †（`providers/mod.rs`）
- `src-tauri/src/services/subscription.rs`
- `src-tauri/src/services/oauth_quota.rs`
- `src-tauri/src/services/usage_stats.rs`
- `src-tauri/src/provider.rs`
- `src-tauri/src/services/provider/**`
- `src-tauri/src/database/schema.rs`
- `src/config/claudeProviderPresets.ts` †
- `src/config/codexProviderPresets.ts` †
- `src/config/geminiProviderPresets.ts` †
- `src/config/universalProviderPresets.ts` †

## 可选审计

- router/share/tunnel 相关协议。
- model catalog，以及 Share/Token Market 自有的售价协议。

## 禁止作为实现来源

- Tauri window/tray/updater。
- React 组件、样式、locale、页面结构和运行时命令。
- Claude Desktop 专属 UI/profile 写入。
- MCP、skills、session manager。
- docs/release notes/assets。

## 审计记录模板

| 日期 | 上游提交/范围 | 分类 | 处理 | 说明 |
| --- | --- | --- | --- | --- |
| 2026-08-11 | Server `Claude Bearer Relay` 与 Custom HTTP 能力复查 | Must Review / Provider Registry 与 Web editor 收敛 | 已按 Server 边界重构 | 删除独立 `family.claude_bearer_relay` / `claude.bearer_relay`，将 Anthropic Messages + Bearer + 模型透传固化为 `claude.custom_http` 下的 `claude.anthropic_bearer_relay` 快捷配方；运行时继续按协议和认证方式确定性分类为 `claude_auth`，出站使用 `Authorization: Bearer`。Registry、Rust/Node coverage audit、Web bundle draft、正式 `web-dist` 与文档同步更新；旧 Profile ID 只作为不可复用 tombstone，不提供读取、迁移或兼容创建入口。 |
| 2026-08-10 | `/data/projects/proxy/Kiro/kiro.rs` 与 Server Kiro OAuth/forwarder/runtime 的只读协议对比 | Must Review / Kiro Claude+Codex 单账号 wire、tool、image、discovery | 已按 Server Share 边界独立实现并强化 | 新增 `assets/contract/kiro-wire-protocol.json` 并由 Rust 测试直接消费；Claude Messages/本地 count_tokens 与 Codex Responses/Chat 通过统一 Anthropic canonical bridge 接入 Kiro，非流和流式生命周期均有本地 fixture。AWS EventStream 对长度、prelude/message CRC、EOF、error/exception frame 严格 fail closed；tool JSON 仅在 `stop=true` 且完整校验后释放，Claude Code 内置工具双向映射；图片执行 MIME/解码/缩放/总量限制。生产 IDE/CLI inference endpoint 固定且 Provider 不可覆盖；实时模型目录替换静态 fallback，缓存按 account id + auth identity generation 隔离，prompt cache 另含 Provider/route/session namespace。每个 Surface 只绑定一个显式账号，401 最多刷新原账号并重放一次；未引入账号池、轮询、分组选号、跨账号目录并集或任何跨账号/跨 Provider 故障转移。Gemini Kiro 不开放，真实账号验收仍为 live pending。 |
| 2026-08-09 | `claude-code-proxy` `4ea0414b5bce26ae38f2547a50d2564ca3d5bc1d`：Kimi auth/model、Grok reducer/stream/accumulate、Cursor model/client 协议证据 | Must Review / Kimi、Grok、Cursor 单账号协议增量 | 已按 Server Share 边界独立实现 | Kimi 采用固定 device/token/coding endpoint、同一 flow 设备身份、JWT `userId` principal、账号级设备头、refresh 身份冲突门禁和 allowlist 模型；Grok 将 hosted web/X search 转成 Anthropic server tool/result/citation/usage 语义；Cursor 增加 Agent/Ask/Plan mode、任意 `-fast` 参数、aliases 和 namespaced catalog。Server 先由 Router Share 固定 Provider 和唯一账号，模型前缀只改变当前 Cursor wire model/mode；401 最多刷新原账号并重放一次。明确不迁移参考实现的 registry Provider 自动选择、账号池、轮换、quota/cooldown/concurrency 选号、错误切号、跨 Provider failover、CLI 本地 token 文件所有权、Skill/MCP/Tauri 或外部 UI。真实 Kimi/Grok/Cursor 账号验收仍为 external gate。 |
| 2026-08-09 | 脱敏 Claude Code `2.1.220` wire capture；Claude profile/roles/model catalog 协议证据 | Must Review / Claude OAuth wire identity、enrichment 与 discovery | 已按 Server 单账号边界独立实现 | 新增 `assets/contract/claude-oauth-wire-profile.json`，只记录 Claude Code/Stainless/Node/Axios 公开版本、endpoint identity family、CCH/beta 规则和模型 ID，不保留 token、账号或原始 body。Server 将 usage/bootstrap/profile/roles/token/inference 身份收敛到版本化 `ClaudeWireProfile`，quota 并行获取 usage/profile/bootstrap/roles 且有界读取；模型发现使用 capture 固定的三模型静态目录、零 OAuth 网络并公开 source/stale/fetchedAtMs。Provider Driver revision 升至 3、discovery 标为 fixture_verified；真实 Share/Max 5x/Max 20x 仍由独立 external gate 验收，不迁移账号池、跨账号调度、外部 UI、Skill/MCP/Tauri 或未被 capture 证明的私有 build suffix。 |
| 2026-08-08 | `codex2api` `6864770c2ba8736cd42404cc0b182bb1e273fecc`：`proxy/codex_invite.go`、`admin/codex_invite.go`、`admin/auto_reset_credits.go`、`admin/reset_credits.go`、`proxy/response_cache.go`、`cache/response_context.go`、`proxy/codex_cli_version_sync.go`、model cooldown 相关实现 | Must Review / OpenAI OAuth referral、credits、continuation 与 client identity 协议证据 | 已按 Server Share 单账号边界独立实现 | Referral 采用固定 ChatGPT endpoint、consumer program、eligibility/send/tracking、动态 grant/capacity、Cloudflare challenge 识别与账号隔离 cookie session，但重新设计为 Provider revision 控制面，始终使用该 Provider 已提交的唯一 OAuth 账号/workspace；不硬编码“1000 credits”权益结论。Personal credits、自动 Banked Reset、模型级 429 cooldown 与 previous-response continuation 均改为 Share 策略，namespace 至少包含 Share/runtime，continuation 进一步包含签名 principal/workspace；Reset 消费使用进程锁、跨进程文件锁和稳定幂等 ID，401 只刷新同一账号并复用同一 ID。Codex CLI release 同步采用启动同步、周期刷新、严格稳定 semver、原子缓存和内置版本下限。明确不迁移账号池、权重/轮换、quota spillover、跨 Provider/model failover、外部 React UI、全局后台扫号、uTLS/TLS impersonation、数据库 schema 或原始上游响应透传。 |
| 2026-08-03 | OmniRoute `e6523da2a12f0eaebc96ba95650efeec2436ab4d` Claude OAuth bootstrap/usage plan evidence | Must Review / Claude Max multiplier protocol evidence only | 按 Server 单账号边界独立实现 | OmniRoute 的 usage 路径把 `oauth_account.organization_rate_limit_tier` 作为 usage `tier` / `plan` / `subscription_type` 之后的计划候选，UI fixture 明确覆盖 `default_claude_max_20x -> Max 20x`，通用解析规则也可识别 `..._5x`。Server 不复制其 UI、连接池、组合或账号调度实现；在独立 domain resolver 中按 usage > bootstrap rate-limit tier > profile rate-limit tier > organization type > compatible cache 的顺序解析，输出规范化 `claude_max_5x` / `claude_max_20x`、安全显示名、stale/conflict evidence 和冲突告警。20x 命名具有本地 checked-in fixture 证据；5x 只有同形协议规则，仍需真实 Max 5x OAuth 账号完成 acceptance 后才能标记 live verified。 |
| 2026-08-02 | 固定 Provider baseline 与当前 Server Registry | Must Review / Provider Profile control plane | 已按 Server 边界补齐 | 保持 upstream commit、五源 hash、15/7/4 preset 和 16 个 ProviderType 快照语义不变；仅在独立 Server migration inventory 中登记 `claude.bearer_relay`、`claude.google_oauth` 两个首类增量。Registry、API creation bridge、Web draft 与 coverage audit 共同要求每个 required `(ProviderType, app)` 至少有一个 visible + `create_allowed` Profile；Google OAuth 统一绑定 `oauth.gemini_code_assist` 和 `gemini_cli` managed identity。此项不引入外部 UI 或实现代码，也不把本地合同通过写成真实 OAuth/反代验收。
| 2026-08-02 | `sub2api` `b74024c7868ee88a0bf921306cbc22a2f922872a` 只读快照；OAuth/API Key、Bedrock、OpenAI WebSocket、Gemini Code Assist `v1internal` 与 401/retry 行为 | Must Review / protocol evidence only | 仅登记协议证据，按 Server 边界独立实现 | 已核对该提交即参考仓库当前 `HEAD`。其中 `gemini_messages_compat_service.go` 将 OAuth `countTokens` 强制分流到 AI Studio `/v1beta/models/{model}:countTokens`，不使用 Code Assist envelope/project；`antigravity/request_transformer.go` 对 Google Search 设置 `requestType=web_search` 并降级到 `gemini-2.5-flash`；`antigravity/stream_transformer.go` 及兼容 service 提供 Claude `message_start`、content/tool block、`message_delta`、`message_stop` 生命周期、function call 的 `tool_use` stop reason，并把 `thoughtsTokenCount` 计入 output usage。其 OAuth token provider、静态 API Key、Bedrock SigV4/request、OpenAI WebSocket relay、在线 project backfill 和错误恢复代码同样只用于核对凭证放置、最终出站认证、响应提交点与同身份恢复语义；不复制 Go service/repository/handler 或账号调度结构。Server 将 `gemini_cli`、`antigravity_oauth`、`agy_oauth` 收敛到共享 Code Assist 生成协议，并独立强化顶层/嵌套 `response.error` 识别、非流与 SSE EOF/`[DONE]` 终态校验、blocked prompt 处理及跨 chunk envelope 解包；只有 Antigravity/Agy 注入其 identity/request 字段，三类 Provider 身份不可混用。缺失 project 时只对显式绑定账号执行账号级 singleflight `loadCodeAssist`，以代际校验即时持久化 project/tier；即使后续 quota 获取失败也保留 project 部分成功，失败进入冷却。首次请求和所有 retry 均固定同一显式绑定账号；下游提交前的首次 401 至多对该账号 refresh/re-resolve 一次，饱和直接 429，受管执行不跨 Provider failover，通用 failover 也不选择受管 Provider 候选。当前仅 fixture/local contract verified，缺真实 Google 凭据与 project entitlement，仍为 live-unverified。`account_pool`、group scheduler、weighted/sticky selection、capacity escape、账号轮换、跨账号重试/故障转移及其计费/倍率系统全部排除，不进入 Server 实现或能力声明。 |
| 2026-07-30 | Codex 官方模型 manifest 结构；cc-switch / CLIProxyAPI FAST、reasoning 与 Responses WS 行为证据 | Must Review / Codex OAuth server-authoritative FAST + reasoning observability | 已按单账号 Server 边界独立实现并强化 | `codexFastMode` 成为唯一 FAST 权威：关闭时最终出站删除客户端 `service_tier`/`serviceTier`，开启时只对 manifest 或内置能力明确支持 `priority` 的最终模型强制注入，明确不支持与能力未知均 fail closed；该策略覆盖 Codex/Claude/Gemini 经 `oauth.openai_codex` 转出的 HTTP/SSE、Chat/Responses/Compact、overflow summary、Images、WebSocket、WS→HTTP fallback 和 Provider 网络测试。推理等级保留客户端 `reasoning.effort` 优先语义及 `low/medium/high/xhigh/max`，只将非 wire 等级 `ultra` 归一为 `max`，不再把 `max` 降为 `xhigh`。普通 Responses 最终出站强制 `store=false` 和上游 `stream=true`，客户端非流请求在 Server 聚合为单个 JSON。usage schema v4、Invoke/Router/UI 分开记录 requested/effective reasoning 与 client/effective tier/decision；流日志以单调 `usageRevision` 从 pending 更新为 observed/missing/parse_error/interrupted，显式 observed zero 不再与未知 usage 混淆，Router 拒绝低 revision 回退；Codex WS 每个 `response.create` 独立创建并在成功、失败、取消、异常或 HTTP fallback 后终结同一 UsageLog。仅借鉴协议字段和能力证据，不迁移倍率计费、美元成本、账号池、轮换或跨账号调度。 |
| 2026-07-26 | 协议证据（Codex 执行器 / 用量 / 图片处理）；本仓库 OAuth Share/Provider/Account 持久化 | Must Review / Claude + OpenAI OAuth Share identity and proxy hardening | 已按 Server Share 边界独立实现并强化 | 采用 Codex `store=false` 下服务端 `rs_`/`fc_`/`resp_`/`msg_` 引用清理、有证据的不兼容 Responses 字段清理、顶层 reasoning effort 提升、显式 code-review quota window 解析，以及远程图片 DNS 全量复验与 pin、逐跳重定向复验、大小/MIME/魔数限制；Server 进一步覆盖 WS/HTTP fallback/Cursor 共用图片边界、IPv4-compatible/NAT64/Teredo/6to4/site-local IPv6 和秒/毫秒 reset。OAuth Share 以 Claude account UUID 或 verified OpenAI subject + effective workspace 为强身份校验绑定完整性；同一强身份可按用户选择保留多个独立 URL，或显式合并为一个 1..=3 app Share。普通 upsert/import 不能换绑，Provider/workspace 变更走 revision-CAS 受控路径，并用 Accounts/Providers/Shares journal 提交与恢复。Share 请求按路由 app 固定对应 Provider 和账号，只允许同账号刷新、同 Provider 重试及 Codex WS→HTTP 原身份回退；明确不迁移外部参考实现的账号池、轮换、随机选择、capacity account fallback、跨账号或跨 Provider failover。 |
| 2026-07-24 | cc-switch `27ce0a51` `650905af` `9ca1a41f` `6d316c0b` `613fef70` | Must Review / cross-protocol tool media, schema, reasoning, streaming, failure semantics | 按 Server 边界独立实现 | 借鉴工具结果媒体原生化、函数 schema 根 object 归一、reasoning 前向附着与 opaque 回放、Anthropic 必需请求字段与工具历史合法化、流式工具身份/顺序、Responses 2xx 失败信封和首业务事件门禁；保留 Server 加密账号、JWT 验签、显式绑定、路由调度和提交边界，不迁移 Tauri、桌面凭据、默认账号回退或上游模块结构。Provider inventory 基线仅在五个权威来源变化时单独刷新。 |
| 2026-07-23 | 协议证据（Claude Code OAuth 工具名大小写） | Must Review / Claude OAuth tool compatibility | 已按证据独立实现并强化 | Claude OAuth 请求将已知 Claude Code 内置工具名按 ASCII 大小写无关方式规范为官方 casing，并在 CCH 签名前同步处理 `tool_choice` 与历史 `tool_use`；流式和非流式响应按请求中声明的原始名字回映。任意自定义工具名保持原样，大小写无关的重复声明因无法无歧义回映而 fail closed；未迁移外部参考实现的 stealth 客户端、单 Provider 凭据模型或工具执行层。 |
| 2026-07-22 | 协议证据（OpenAI OAuth 与客户端传输）；协议证据（Codex 远程 callback 与 Responses WS）；既有 Server OAuth/proxy | Must Review / OpenAI OAuth transport（历史路由边界已取代） | 已按证据独立实现并强化 | 明确外部参考实现仅作为官方 OAuth/客户端行为参考，不作为多租户 Server 架构模板。Server 保留 Device OAuth，并为远程 HTTPS Client URL 增加固定 localhost redirect 的完整 callback URL 手工回传；浏览器和 device flow 均绑定管理员主体及有效期。OpenAI ID/access JWT 统一走 JWKS/RS256/issuer/audience/time 验证与 canonical identity，要求独立 subject/workspace 并用 subject 哈希作为本地账号 ID；控制面账号脱敏，managed endpoint 固定且账号必须显式绑定。HTTP/SSE/Images/WS 首次 401 只对同账号强刷一次；本条早期记录曾包含的负载、quota、session affinity 和未固定 Provider failover 已被 2026-07-26 与 2026-07-30 的单账号边界取代，当前 Codex OAuth 不做账号池、跨账号或跨 Provider 调度。Codex WS 使用隔离的有界连接池、首业务事件/空闲分段超时及提交前同身份 HTTP/SSE fallback，握手 4xx、空闲超时与已提交响应不重放。未迁移外部参考实现的单 Provider 文件模型与管理面或主机 `~/.codex/auth.json` 所有权。 |
| 2026-07-22 | cc-switch、外部参考实现的 Provider 出站 User-Agent/客户端身份实现 | Must Review / Provider outbound identity | 已按证据独立实现并收口 | Provider Registry schema v2 为每个 Driver 声明受管、Server、omit、custom override 或 frozen legacy 策略；只有 Custom HTTP 可配置 User-Agent，预设 Provider 在认证和账号 header 后执行最终身份覆盖。Claude/Codex/Grok 与 Google Code Assist OAuth、Kiro/Cursor/Copilot/DeepSeek、Antigravity/agy 分别使用协议身份，普通 HTTP/API-key 使用 Server 身份，Bedrock 不发送 UA。Antigravity 版本由官方 updater manifest 后台刷新，失败使用缓存/内置版本且不阻塞请求。HTTP、WS、图片/媒体、测试、模型发现与 Share 健康检查共用同一运行时策略；未迁移外部管理页面或全局可编辑 UA 设置。 |
| 2026-07-21 | Provider Profile/Driver/RuntimePlan 与 Server-native editor 收口 | Must Review / Provider control plane | 已选择性重构 | Rust `ProfileSpec`/`DriverSpec` 成为身份和能力权威，forward/test/discovery 共用 canonical RuntimePlan；Provider 写入统一使用 revision、CredentialPatch 和 persist-before-swap command。全新安装写 S2，旧 S1 只通过停服 CLI 显式迁移并保留回滚快照。Server Add/Edit 只使用本仓库的 `ServerProviderForm`；外部 UI 分支不进入 production graph，也不作为后续实现参考。S1/name/URL reader 与三个 compatibility endpoint 继续受“两次稳定发布且不少于 14 天”门禁保护。 |
| 2026-07-20 | `b1dee0153da94316fb50416c679a11f74cc66f14` 五个 Provider 权威来源 | Must Review / Provider baseline | 已固定受控快照 | TypeScript preset 使用静态 AST 抽取，不执行上游代码；记录 Claude/Codex/Gemini 15/7/4、Universal 2 和 ProviderType 16。当前上游工作树 dirty 且含冲突，只允许从固定 commit 读取 Git blob，禁止从工作树刷新。Server 独有 Grok profile 继续进入独立 16/8/5 migration inventory，等待 Rust ProfileSpec registry 接管。 |
| 2026-07-27 | `grok2api` CLI adapter/config 的 Grok OAuth 协议证据：可选 turn 透传、CLI 0.2.111、workspace scopes、device client headers | Must Review / Grok OAuth single-account transport | 已按证据独立实现并强化 | 默认身份更新为 `grok-shell/0.2.111`；browser/device OAuth 加入 `workspaces:read`、`workspaces:write`，device start/poll 携带 client version/surface，poll 复用受 x.ai allowlist 约束的 token URL。`x-grok-turn-idx` 只在下游提供合法十进制 u64 时透传，401 重放及 WS→HTTP fallback 复用原值，未提供或非法时省略，server 不再维护或推断 turn。HTTP/SSE、媒体和 WS 均只允许同一 Provider 的单一绑定账号做一次强制 refresh，不进入通用 Provider/账号 failover；capability 默认 fail-closed，成功证据持久化。仅测试构建允许 loopback WS/models driver option，不开放生产自定义上游。 |
| 2026-07-20 | 外部参考实现 Grok CLI identity and non-numeric quota state；外部参考实现 weekly+monthly billing merge；外部参考实现 subscription/task usage and credit-bag parsing | Must Review / Grok OAuth quota + subscription metadata | 已按证据独立实现并强化 | Grok quota refresh 统一使用共享的可覆盖 Grok CLI 指纹（当前默认 `grok-shell/0.2.111`），并并发探测 user、weekly billing、monthly billing 和 task usage，缺订阅到期证据时 best-effort 查询 `/rest/subscriptions`。新增 weekly/product/monthly/task/credit-bag 规范化、per-probe status/warning/stale 合并与 402 spending-limit 状态；API/UI 区分 numeric、non-numeric、partial、unavailable，并禁止将 token expiry、`currentPeriod.end` 或 `billingPeriodEnd` 当作订阅到期。只接受 active subscription 内明确的 expiry 字段，否则允许按账号手动设置下一次付款到期时间；手动值在 OAuth 刷新后保留并同步到 provider/Share 元数据，但不影响凭据有效性或代理调度。不迁移 grok.com web-cookie 逆向、Skill/MCP/Tauri/Desktop 能力。 |
| 2026-07-20 | 外部参考实现 `backend/internal/service/openai_privacy_service.go` ChatGPT account candidate filtering；本地生产 `/wham/usage`、`/accounts/check` 诊断证据 | Must Review / Codex OAuth quota + subscription metadata | 已按证据独立实现并强化 | 根据证据实现 expired/deactivated/disabled/inactive/suspended account candidate 过滤；Server 进一步将 `/wham/usage.plan_type` 设为展示套餐权威，只允许已验证 workspace 的 `/accounts/check` 与 `/subscriptions` 提供到期日，禁止可信 workspace 精确匹配失败后跨账号回退。套餐冲突、无可信 workspace 的到期日、以及付费 usage 仍可用时的历史过期日均丢弃，并持久化不含凭据的裁决证据；刷新后的空到期日会同步清除 Auth 与 Share descriptor 的错误 expired 状态。外部参考实现的代理/TLS/管理面实现保持排除。 |
| 2026-07-19 | 协议证据（Claude）最新对比；外部参考实现 beta policy；外部参考实现 parallel tool/SSE lifecycle；Anthropic profile billing metadata | Must Review / Claude contract + count_tokens + stateful stream + profile | 已按证据独立实现 | Claude OAuth contract 改为最终 body 清理后统一重算 CCH；thinking sampling 冲突归一，body/header beta 经 request-shape allowlist 决策并增加固定枚举指标，account extra headers 不能覆盖签名契约。新增 `/v1/messages/count_tokens` 与 `/claude/v1/messages/count_tokens`，只调度 `claude`/`claude_auth`/`claude_oauth` 原生 Anthropic provider，不做跨协议近似且不写生成 usage。通用跨协议 SSE 增加每请求 framing 和 Responses/Chat→Anthropic 并行 tool lifecycle，覆盖 packed arguments、任意 chunk 切分、CRLF 和 EOF 半帧。Claude profile 请求复用现有并发分支，将 `billing_type` 写入 `profile.billingSource` 并保留未知值，不推断 plan/expiry。Skill/MCP/Tauri/Desktop profile、wire casing、uTLS/JA3、tool cloaking 继续排除或证据门控。 |
| 2026-07-13 | 外部参考实现 `beta_policy.go`；外部参考实现 OAuth session cancellation/idempotent completion commits | Must Review / Claude OAuth beta + login session hardening | 已按证据独立实现 | Claude OAuth 不再无条件合并客户端 `anthropic-beta`：只允许协议内置 beta 与精确审计的 `prompt-caching-2024-07-31`、`token-efficient-tools-2025-02-19`，未知/实验 token 丢弃并写 debug 日志；非 OAuth Claude 路径保持原兼容行为。现有 server-native `OAuthLoginStore` 增加原子且幂等的 Cancelled 终态、认证 cancel API、Web invoke 接线、完成账号 ID 留存与重复完成返回；未知 state 继续 404，exchange 已启动后拒绝取消以避免授权码已消费但账号未落库。管理员 BetaPolicy 配置化、wire casing、TLS/JA3、tool cloaking 继续证据门控。 |
| 2026-07-13 | 协议证据（Codex） v2；外部参考实现 identity/tool_search/cache/WS fixes；外部参考实现 additional/custom tools and output restoration；外部参考实现 token authority/WS toggle | Must Review / Codex OAuth + proxy hardening | 已按证据独立实现 | Codex 出站身份统一为配对的官方 family，默认 `0.144.1`；device poll 可取消、幂等并防并发 exchange；refresh token 由 server 独占、重复导入拒绝且 replay 立即 relogin；OpenAI `id_token` 增加 JWKS/issuer/audience/exp 验签；workspace 只能从验证 claims 选择。补 Responses Lite additional/custom tools、tool_search 冲突/强制降级、custom 流回放、SSE/WS output 修复、wire 零值、cache-write、GPT-5.6 capability gate、非法 message item ID 清理、Windows WS reset 和 per-provider WS 回退。Images 路由沿用既有实现。TLS/JA3 继续证据门控；不读取/写穿 server host 的 `~/.codex/auth.json`，不迁移 Skill/MCP/Tauri/session manager。 |
| 2026-07-13 | 协议证据（Claude） 2026-07-13 增量；外部参考实现 transport failover、外部参考实现 cache usage、外部参考实现 bootstrap、外部参考实现 response headers | Must Review / Claude OAuth + proxy hardening | 已按证据独立实现 | Claude 数据面仅接受 Router 签名的 Share ingress，并固定 Share binding 解析出的 Surface 及其绑定账号，不实现跨 Provider/账号自动故障转移。Messages 只允许一次 connect-stage 重放、一次同账号 401 强刷重放及有界 OAuth body 兼容降级；不明确的 send/timeout/body-read、429/529、SSE error 和提交后失败均不透明重放。`count_tokens` 只可在原身份上有界重试非生成型传输失败。原生 Anthropic JSON/SSE 增加增量语义守卫，客户端取消不惩罚 Provider health。Claude bootstrap 在登录交换后和 quota refresh 时 best-effort 拉取，仅保存规范化账号/组织字段。Anthropic/OpenAI/Gemini usage 统一为 fresh input、cache read、cache creation、output 四桶语义并补齐流/非流转换；安全响应头 allowlist 增加 request id、retry/rate-limit/priority/fast 系列。未迁移 Skill/MCP/Tauri/session manager/Desktop profile；wire header ordering、TLS/JA3、tool cloaking、54547 listener 与 MITM 继续证据门控。 |
| 2026-07-13 | `kiro.rs` `31aefbe` + `b304466` Kiro tool schema / tool JSON hardening | Must Review / Kiro proxy defense | 已按证据独立实现 | Kiro tool inputSchema 顶层强制 `type=object`，剥离 `oneOf`/`anyOf`/`allOf` 并从首个 object variant 恢复字段；非流式 toolUse 分片按 id 累积并在 `stop=true` 后解析，非法/半截 JSON 分别返回 `TOOL_JSON_INVALID` / `TOOL_JSON_INCOMPLETE` 502；`TOOL_SCHEMA_INVALID` 与 `TOOL_USE_RESULT_MISMATCH` 明确作为客户端终态校验错误，跳过 retry/Provider outcome 记账；补 `ksk_` API Key 错误/日志掩码。可选 toolCompatibilityMode 与 `e0e77c7` token_manager persist_lock 保持排除，后者如有 Server 需求必须按 state/debounced save 所有权重新设计。 |
| 2026-07-10 | 协议证据（Kiro） P0-P2；kiro.rs/外部参考实现 Kiro OAuth + CodeWhisperer proxy references | Must Review / Kiro OAuth + proxy | 已按证据独立实现 | server 补齐 Kiro native refresh（Builder ID/IdC/Social/External IdP 分流）、Google/GitHub Social device flow、OIDC refresh 401 client re-register fallback、Kiro credentials.json/API key 导入与 ListAvailableProfiles 校验、IdC issuerUrl 持久化、External IdP Microsoft endpoint allowlist、嵌套凭据字段加密、getUsageLimits 回填、profileArn/region 兜底、API_KEY/EXTERNAL_IDP tokentype header、CLI endpoint 请求形态、EventStream CRC 校验和 inline `<thinking>` 拆分；当时保留 Claude-only Kiro forwarder capability planned，该历史状态已由 2026-08-10 的 Claude/Codex fixture-verified Native 实现取代；明确不迁移 Kiro IDE/Tauri/MCP/skills/session manager 能力。 |
| 2026-07-09 | 协议证据（Cursor） P0-P2 + §6/§7 增量审查；Cursor OAuth/AgentService；外部参考实现 Cursor import + AgentService references | Must Review / Cursor OAuth + proxy | 已按证据独立实现 | Server 补齐 Cursor 本机导入（IDE `state.vscdb` 优先、`CURSOR_STATE_DB_PATH` override、immutable SQLite URI、三平台 cursor-agent `auth.json` + `CURSOR_AGENT_AUTH_PATH` 兜底）、WorkOS Cookie `/api/auth/me` profile enrichment、Cursor AgentService 默认 native 启用并保留显式禁用开关、Provider capability 从 planned 调整为 native；根据 §6/§7 证据实现 Cursor token UA、CLI client version 探测与 fallback、AgentService trace headers、timezone、稳定账号 ID、429 cooldown、8KB JSON 错误诊断和图片 URL 防护；Skill/MCP/Tauri/session-manager/Claude Desktop profile 能力保持排除。 |
| 2026-07-09 | 协议证据（Grok） P0-P2；外部参考实现 Grok OAuth + xAI proxy references | Must Review / Grok OAuth + proxy | 已按证据独立实现 | server 新增 `grok_oauth` provider type 覆盖 Claude/Codex/Gemini；补齐 xAI OAuth public client、96B PKCE、x.ai endpoint allowlist、Grok JWT/profile enrich、`~/.grok/auth.json` 导入、Responses/Chat/Images/Videos/Models 反代、xAI header/session 合约、body 清洗、reasoning/tool/encrypted_content 校验、WS Responses bridge、视频 sticky session、401/403/429/5xx 账号冷却和 Grok-only 媒体 provider 选择；明确不迁移 grok.com web cookie 逆向、Skill/MCP/Tauri/Desktop 客户端能力 |
| 2026-07-10 | 协议证据（Claude） P0-P2 + 二/三/四/五/六轮 review；外部参考实现 Claude OAuth + proxy references | Must Review / Claude OAuth + proxy（历史账号选择已取代） | 已按证据独立实现 | server 补齐 Claude OAuth refresh singleflight/退避、后台 warm-refresh 与连续 `invalid_grant` 自动隔离、`prompt=login`、统一 Claude CLI UA/CCH 常量且 CCH 默认 `cc_entrypoint=cli`、CCH seed env override、per-account stainless OS/arch profile、stream-sensitive stainless timeout、CLI header/session/user_id 合约、session-id first-user-text 种子、动态 `anthropic-beta`、非 CC system prompt 重写、billing block dedupe/TTL override、serde_json preserve_order wire 保序、缺省 `tools: []`/`max_tokens`/`temperature`/thinking `context_management`、上游 `x-request-id` 透传、Claude OAuth per-account concurrency guard（默认 8，可配置/关闭）、SSE 首 chunk `event:error` Provider outcome 信号、同身份有界 retry、400 signature/thinking 反应式降级重试、web_search 历史块过滤、Claude CLI version-gate admin rewrite、Prometheus `/metrics`、Claude CLI callback route、Claude credentials 显式 import/export。该条早期记录中的“按占用比例账号选择”已经废止：当前首次请求与 retry 均固定 Share binding 的显式绑定账号，并发饱和直接 429，不换号且不进入跨 Provider failover。明确不迁移 Claude Web UI 反爬、MITM、Skill/MCP/Tauri/Desktop profile 自动写入，独立 54547 listener 后续按产品需求评估。 |
| 2026-07-09 | 协议证据（Codex） P0-P2；外部参考实现 `patchCodexCompletedOutput`；外部参考实现 instructions；Codex WS/header 行为证据；外部参考实现 429/UA references | Must Review / Codex OAuth + proxy（历史账号跳选已取代） | 已按证据独立实现 | Server 补齐 Codex OAuth CLI callback、per-account refresh lock、accounts token 字段 AEAD 加密和 `accounts.key` 备份、429 `resets_in_seconds`/`resets_at` 冷却记录、Codex CLI UA/originator/version 头、Responses GET WebSocket 桥、版本化 instructions 注入、`response.completed` output 回补、JWT claim profile enrich。该条早期记录中的“跳过限流账号”已经废止：管理员必须选择唯一活动账号，首次请求与所有 retry 固定该显式账号，429 只更新该账号的 cooldown，不换号且不进入跨 Provider failover。Skill/MCP/Tauri 能力保持排除。 |
| 2026-07-07 | Claude OAuth hot path 与 web-paste token exchange 行为证据 | Must Review / proxy + oauth | 已独立实现 | `src/proxy/claude_oauth.rs` 补齐 `beta=true`、`anthropic-beta`、billing `system` 注入、`cch` 签名；`domain/accounts/oauth.rs` + `clients/oauth/refresh.rs` 补齐 web-paste `code#state` 解析、platform token 优先与 UA；`docs/provider-coverage.md` evidence notes 已更新 |
| 2026-07-07 | `d7d33e51` Ollama Codex reasoning effort clamp | Must Review / proxy | 已独立实现(X2) | Codex Responses→OpenAI Chat 上游归一时，Ollama 目标按协议枚举映射 `xhigh→max`、`minimal→low`，并保留显式 `none/off/disabled→none`；非 Ollama 目标继续透传 |
| 2026-07-02 | `d73527f1` Codex chat completions bridge for OAuth responses | Must Review / proxy | 已独立实现(A6) | `src/proxy/transforms.rs` 新增 Codex/OpenAI Chat↔Responses 直接请求、响应和 SSE 桥接；Codex OAuth `/chat/completions` 上游归一保留 max/reasoning/response_format/tool/usage 字段 |
| 2026-07-02 | `273cc48c` Codex CN provider native Responses | Must Review / provider routing | 已独立实现(A6) | `scripts/audit-provider-coverage.mjs` preset 来源切到 official，上游 `openai_responses` CN preset 进入 `docs/provider-coverage.*`；server 继续由显式 `apiFormat` 驱动 Responses/Chat 路由 |
| 2026-07-02 | `784d35bd` `62c1d77e` `e1ddd86e` `8e680164` `d79fee5b` Cursor AgentService/SSE fixes | Must Review / Cursor adapter | 部分独立实现(C1) | server 已有显式 opt-in Claude/Codex/Gemini 文本/图片/声明工具 AgentService h2 driver、Cursor API Key exchange、AgentService endpoint override、stream interrupted usage 更新、MCP/built-in tool bridge 和 tool_result 同 h2 stream park-resume；真实 Cursor 回归仍随 C1 收尾 |
| 2026-07-02 | `cb306e95` Ollama renewal in share sync | Must Review / quota/share sync | 已独立实现(A4) | Ollama `/api/me` refresh 写入 subscription period，share descriptor/runtime snapshot 输出 `subscriptionExpiresAt`/`subscriptionRemainingMs` |
| 2026-07-02 | `3a7ae36e` `c0fbe902` Ollama quota display-only and display fix | Must Review / quota | 已独立实现(A4) | Ollama Cloud `supportsQuota` 改为 provider-specific，quota 不生成 fake `quotaPercent`，展示订阅等级为 `ollama <plan>` |
| 2026-07-02 | `e3968b72` quota summary subscription expiry | Must Review / quota | 已独立实现(A4) | Codex/Ollama 订阅到期写入 `quota.extraUsage` 并进入 share descriptor；真实账号 smoke 归入 F2 |
| 2026-07-02 | `6d695fe2` Codex banked reset credit time | Must Review / quota | 已按 Share 单账号边界实现 | Banked Reset 明细进入账号 quota；Provider 控制面可手动消费，Share 可选择临期自动消费。自动路径只重读并使用当前 Share 绑定的单一账号/workspace，具备稳定幂等、进程/文件双锁、同账号一次 401 refresh 和成功后强制 quota refresh，不扫描或切换账号。 |
| 2026-07-02 | `ab09b1f7` share route/provider metadata improvements | Must Review / share descriptor | 已独立实现(A1) | 已补 share invocation guard、usage counters 和 descriptor counters；后续真实 direct/share metadata 差异随 F2 验收复核 |
| 2026-07-02 | `88afe26e` share request country metadata sync | Must Review / request log | 已独立实现(A6) | `src/proxy/forwarder.rs` 读取 `x-cc-switch-user-country`/`x-user-country` 与 ISO3 header，`state.rs` request-log batch sync 已覆盖 country 字段 |
| 2026-07-02 | `430ddf92` `dd6a951c` `de386b29` SubRouter/OpenCode Go presets | Must Review / presets | 已独立实现(A6) | official preset coverage 已重生成，`docs/provider-coverage.*` 包含 SubRouter 和 OpenCode Go；`docs/provider-fixtures/structures.json` 已按当前 cc-switch fork 重跑 |
| 2026-07-02 | `e6d40d0a` OpenCode Go referral/promo preset copy | Must Review / presets | 已独立实现(A6) | 固定 Provider 审计快照记录 OpenCode Go referral/promo 元数据；Server 不复制外部前端 promo 文案组件。 |
| 2026-07-02 | `d1f6c74b` usage_script credentials as explicit overrides | Must Review / provider service | 明确跳过 | Server 当前只保留 `ProviderMeta.usage_script` 原始 JSON，不执行外部 usage script，也不写 live provider config；未来只有出现明确 Server 需求时才可按本仓库契约重新设计。 |
| 2026-07-02 | `05da23e1` model-test Share path Codex stream false-positive fix | Must Review / model health | 已独立实现(A5) | `src/core/model_health.rs` 从 health-check usage 派生 summary，流式记录必须 `streamStatus=completed` 才计 `success`，避免 Share 路径 Codex stream false-positive |
| 2026-07-02 | `778d5b92` Claude Sonnet 5 pricing | Optional Review / pricing | 已拒绝并从产品边界删除 | Server 不计算模型成本，不保留默认模型定价或促销价；Token Market 售价由 Share/Market 自有协议维护。 |
| 2026-07-02 | `cd9e025b` `76b8620f` Sonnet tier default/test alignment | Must Review / presets | 已按证据独立实现(G6) | 固定 Provider 审计快照记录 `claude-sonnet-5` 默认档；Server registry 和 fixture 按本仓库契约维护。 |
| 2026-07-02 | `9079935d` NekoCode, `a8657d22` Code0.ai, `332a3c16` Amux presets | Must Review / presets | 已按证据独立实现(G6) | 固定 Provider 审计快照记录 NekoCode/Code0.ai/Amux；Server coverage 由 `audit-upstream-provider-baseline.mjs` 的受控 AST 证据和本仓库 registry 共同校验。 |
| 2026-07-02 | `52a0fb4c` external merge evidence | Mixed / historical review | 已登记(G6) | Server 不整仓 merge；模型成本已从产品边界删除，其他差异只有形成明确 Server 需求和协议证据后才单独实现。 |
